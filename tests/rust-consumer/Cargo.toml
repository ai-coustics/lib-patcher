[package]
name = "rust-consumer"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
# We intentionally use different versions of dependencies than testlib
# to ensure symbol conflicts would occur without patching
rand = "0.9"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[build-dependencies]
